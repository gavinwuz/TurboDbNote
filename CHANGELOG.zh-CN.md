# 更新日志

[English](CHANGELOG.md) | 简体中文

版本变化统一记录在此；发布说明从对应版本章节生成。候选版本不代表已经公开发布。

## [Unreleased]

### 修复

- Windows 打包通过显式编译器参数与隔离 GPUI 构建适配解决 `Failed to find fxc.exe`，不使用自定义环境变量。

### 新增

- 绿色 ZIP 同时提供 Inno Setup EXE，支持双语安装、当前用户快捷方式、卸载及运行中升级保护；不再计划支持 Windows 7。
- 40px 单行标题栏，整合产品标识、布局/主题按钮和窗口控制。

## [0.1.0]

预览候选版，公开发布日期尚未确定。

### 新增

- 极简图标导航、可调整四区 Dock 布局、辅助面板标签及专注模式。
- 当前会话内 Dark / Light 主题切换。
- SQL、Markdown、任务子项的内存编辑、折叠及任务状态切换。
- 版本化 `.note` 模型、独立查询状态及数据库/AI 服务契约。
- 数据笔记产品标识、Windows 应用图标与版本信息。
- Windows x64 便携 ZIP 打包、构建信息、SHA256 校验和及候选制品流水线。
- 英文与简体中文 README、更新日志及自动生成的双语发布说明。

### 修复

- SVG 白底预览不可见；补齐多尺寸 EXE 图标和显式窗口大小图标。
- 已确认从其他目录启动可解决观察到的任务管理器分组图标问题；旧路径缓存仍可能需要重启应用。

### 已知限制

**编辑内容仅保留在内存中，关闭程序后丢失。请勿用于保存正式工作内容。**

尚未提供文件保存、真实数据库执行、AI 集成、完整 SQL 补全与 Markdown 预览。主题选择不跨重启保存，未提供自动更新，性能目标未经过正式基准验收。候选程序未签名；干净 Windows VM 验收和第三方许可审核仍为公开发布前置条件。
